import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function AiCoachPage() {
  return (
    <PagePlaceholder
      title="AI Coach"
      phase="M9–M10 (AI Foundation + Coaching)"
      description="Personalized coaching — risk, behavior, strategy, and performance. AI explains; it never forces."
    />
  );
}
