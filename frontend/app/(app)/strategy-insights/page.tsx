import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function StrategyInsightsPage() {
  return (
    <PagePlaceholder
      title="Strategy Insights"
      phase="M10 (Strategy Intelligence)"
      description="A strategy research terminal — when each strategy works best, and what to avoid."
    />
  );
}
