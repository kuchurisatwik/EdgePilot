import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      phase="M1 (Auth + Settings)"
      description="Profile, account size and default risk %, strategies, and risk rules."
    />
  );
}
