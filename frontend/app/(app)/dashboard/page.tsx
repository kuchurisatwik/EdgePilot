import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      phase="M6 (Analytics)"
      description="Overall account health — balance, today's PnL, risk exposure, trade score, drawdown."
    />
  );
}
