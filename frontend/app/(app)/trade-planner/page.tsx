import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function TradePlannerPage() {
  return (
    <PagePlaceholder
      title="Trade Planner"
      phase="M3 (Trade Planner + Risk Engine)"
      description="Validate a trade before execution — strategy, entry/stop/target, and always-visible risk analysis."
    />
  );
}
