import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function AnalyticsPage() {
  return (
    <PagePlaceholder
      title="Analytics"
      phase="M6 (Analytics)"
      description="Performance intelligence — win rate, profit factor, expectancy, drawdown, and equity curve."
    />
  );
}
