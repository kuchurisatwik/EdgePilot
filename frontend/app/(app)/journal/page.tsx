import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function JournalPage() {
  return (
    <PagePlaceholder
      title="Journal"
      phase="M5 (Journal)"
      description="Your trade database — filter by strategy, symbol, date, win/loss, and session."
    />
  );
}
